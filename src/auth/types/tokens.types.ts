export interface Tokens {
  access_token: string;
  refresh_token: string;
}

export interface AtTokens {
  sub: string;
  role: string;
  username: string;
  sessionID: string;
}

export interface RtTokens {
  access_token: string;
}
